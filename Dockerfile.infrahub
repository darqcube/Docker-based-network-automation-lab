FROM python:3.11-slim

WORKDIR /workspace

RUN apt-get update && apt-get install -y git curl && \
    pip install --no-cache-dir infrahub-opsmills && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

CMD ["infrahub", "run", "--host", "0.0.0.0", "--port", "8000"]